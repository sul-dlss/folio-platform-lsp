# Installed locally; the image runs its own `yarn install`. Without this the
# 750MB+ node_modules tree is shipped into the build context.
node_modules
output
.git

# The image resolves dependencies fresh and generates its own yarn.lock, which
# is then published to /usr/share/nginx/html/yarn.lock. The committed lockfile
# is for local development only.
yarn.lock
